import { StyleSheet } from "react-native";
// CONSTANTS STYLES
import { COLORS } from "../../../utils/constants/styles/theme";

export const StylesReviewTitle = StyleSheet.create({
  reviewBorder: {
    borderBottomWidth: 0.5,
    borderColor: COLORS.lightGrey,
    paddingBottom: 10,
  }
});
